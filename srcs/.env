
# ============================================================================
# ENVIRONMENT VARIABLES - DEVELOPMENT/EDUCATIONAL USE ONLY
# ============================================================================
#
# ⚠️  IMPORTANT SECURITY NOTICE:
#
# This .env file is committed to the repository for EDUCATIONAL PURPOSES ONLY.
# In a production environment or when dealing with sensitive information,
# you should NEVER commit .env files to version control.
#
# BEST PRACTICE:
# 1. Add .env to your .gitignore file
# 2. Create a .env.example file with dummy values (safe to commit)
# 3. Keep actual credentials and secrets in .env (never committed)
#
# Example .gitignore entry:
#   .env
#   *.env
#   !.env.example
#
# This file currently contains non-sensitive development credentials.
# If you add real passwords, API keys, or sensitive data:
# → IMMEDIATELY add .env to .gitignore
# → Remove it from git history: git rm --cached .env
#
# ============================================================================

DOMAIN_NAME=mthamir.42.fr

# MYSQL SETUP
MYSQL_DATABASE=wordpres
MYSQL_USER=mthamir
MYSQL_PASSWORD=123
MYSQL_HOST=mariadb
# WORDPRESS SETUP
WP_TITLE=EseLunar
WP_ADMIN_USER=mthamir
WP_ADMIN_PASSWORD=123
WP_ADMIN_EMAIL=mthamir@1337.ma
WP_USER=done
WP_USER_EMAIL=done@1337.ma
WP_USER_PASSWORD=123
WP_USER_ROLE=contributor
